Stop changing the half-wall implementation for now.

Six independent fixes have changed real defects without changing the visible
symptom. The next step is runtime instrumentation, not another hypothesis.

==================================================
GOAL
==================================================

Determine what the CENTRE RAY actually sees at runtime.

We need to measure:

    MI2
    HC(MI2-1)
    WH before HALF processing
    WH after HALF processing
    SY1
    SY2

Do this in the existing debug/readout area if possible.

Do NOT change the rendering logic while adding this diagnostic.

==================================================
1. INSTRUMENT ONE RAY ONLY
==================================================

Use the centre ray / centre screen column.

Do not add debug work to all 128 rays.

At the point immediately AFTER the DDA has completed for the centre ray, capture:

    hit cell index: MI2
    half flag:      HC(MI2-1)
    wall type:      WV
    distance:       D

Then around the existing half-height calculation capture:

    WH before:
        HWF=WH

    existing:
        WFB=INT(112+WH/2)
        IF HC(MI2-1)=1 THEN WH=INT(WH/2)

    WH after:
        WH

    final:
        SY1
        SY2

Display these values on screen.

Something equivalent to:

    MI2 47 HC 1 WV 2
    WHF 180 WH 90
    SY1 112 SY2 202

is sufficient.

Use whatever existing text/debug primitives are already proven.

==================================================
2. DO NOT ASSUME CELL 46 IS THE RAY HIT
==================================================

The editor/generator has already proven:

    halfCells[46] = true
    HC(46)=1

But that does NOT yet prove the ray looking at the visible wall hits MI2=47.

That is what this test must establish.

Remember:

    HC index = MI2-1

Therefore if the centre ray is visibly aimed at the painted HALF cell, the
runtime diagnostic should eventually show:

    MI2 = 47
    HC(MI2-1) = 1

If it instead shows another MI2, stop there.

Do not alter HC indexing to compensate until we understand why the ray is
reporting another cell.

==================================================
3. INTERPRET THE RESULT MECHANICALLY
==================================================

CASE A:

    MI2 = expected cell
    HC = 0

Then the rendering code is NOT the problem.

The array does not contain the generated value at runtime.

Investigate HC initialization / generated assignment / array behaviour.

Specifically verify execution reaches:

    HC(46)=1

and that reading HC(46) immediately after initialization returns 1.

Do not change projection.

--------------------------------------------------

CASE B:

    MI2 != expected cell

Then the static assumption about MI2/hit-cell correspondence is wrong in the
actual runtime path.

Inspect exactly where MI2 is assigned relative to:

    DDA stepping
    wall detection
    EXIT FOR
    any door/thin-wall special cases

Do not change DDA mathematics.

We only want to establish which variable actually retains the accepted hit
cell.

--------------------------------------------------

CASE C:

    MI2 correct
    HC = 1
    WHF e.g. 180
    WH e.g. 90

Then HALF processing is definitely executing.

Now inspect:

    SY1
    SY2

They must reflect the shortened WH.

If they do, HC and WH are innocent.

--------------------------------------------------

CASE D:

    HC = 1
    WH halves
    SY1/SY2 are correct
    BUT wall remains visually full-height

This is extremely important.

At that point STOP investigating HC entirely.

It proves the problem is downstream of projection.

Then audit the FINAL GENERATED BAS for the centre wall strip and determine
whether the actual visible sprite is the sprite whose:

    SPRPOS2 ...,SY1,...,SY2,...

we are modifying.

Search for every write to that sprite ID:

    SPRTEX
    SPRPAL
    SPRPOS
    SPRPOS2
    SPRDISP
    SPROFF

and determine whether another command later in the frame restores/replaces its
geometry.

Also verify there isn't a second wall-rendering layer underneath the textured
strip making the supposedly shortened region appear filled.

Do not infer this from the template.
Inspect FINAL GENERATED BAS.

==================================================
4. TEST HC ARRAY DIRECTLY
==================================================

Because multiple arrays are a remaining unknown, add one cheap diagnostic
outside the raycaster after initialization:

    HC(46)

It should display/read as:

    1

This separates:

    generator emitted HC(46)=1

from:

    interpreter actually stored HC(46)=1

If HC(46)=0 immediately after initialization, we have found the failure before
the raycaster even begins.

==================================================
5. DO NOT JUMP TO "THREE ARRAYS ARE UNSUPPORTED"
==================================================

Current arrays include:

    DR(0 TO 195)
    ZB(0 TO 127)
    HC(0 TO 195)

We do NOT have evidence that EZ-BASIC limits the number of arrays this way.

Do not redesign storage based on that possibility.

Measure:

    HC(46)

first.

If it reads 1, the multiple-array theory is eliminated immediately.

If it reads 0, isolate array behaviour with the smallest possible diagnostic
before changing the game architecture.

==================================================
6. CHECK THE GENERATED INITIALIZATION ORDER
==================================================

Inspect FINAL BAS and report the exact ordering of:

    DIM HC(...)
    HC zeroing FOR/NEXT
    HC(46)=1
    game/raycast loop

Make absolutely sure the emitted:

    HC(46)=1

occurs AFTER:

    FOR DI=0 TO 195
      HC(DI)=0
    NEXT DI

and that the zeroing loop is not executed again later.

This is worth checking because the source text containing HC(46)=1 is not
enough if initialization order wipes it afterward.

==================================================
7. IMPORTANT: DO NOT CHANGE THESE
==================================================

Do not change:

- BSIM/BSPL writers
- DDA mathematics
- WXF/TXC
- atlas allocation
- z ordering
- half-wall indexing
- projection formula
- HC storage representation
- sprite IDs

Do not add another proposed "fix".

This build is now a measurement build.

Inline IF only.
132 characters maximum INCLUDING line number.
No block IF.
Do not use SPROFF for wall-strip hiding.

==================================================
8. REPORT THE ACTUAL NUMBERS
==================================================

Return the observed centre-ray values, not just "looks correct":

    expected halfCells index =
    HC(expected index) after initialization =

    MI2 =
    MI2-1 =
    HC(MI2-1) =
    WV =
    D =

    WH before =
    WH after =
    WFB =
    SY1 =
    SY2 =

Also provide the exact final generated BASIC lines for:

    DIM HC
    HC zeroing
    HC(46)=1

and the centre-ray HALF/projection/SPRP0S2 block.

==================================================
DECISION POINT
==================================================

Do not proceed to another fix until those values are known.

The diagnostic should tell us which boundary contains the failure:

    generated state
        ↓
    runtime HC storage
        ↓
    DDA hit identity
        ↓
    HALF branch
        ↓
    WH
        ↓
    SY1/SY2
        ↓
    actual visible sprite

We have already spent six fixes reasoning across that entire chain.

Now locate the exact boundary experimentally.
