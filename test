STOP CHANGING THE SCENE TRANSFORM IMPLEMENTATION.

We have now made six changes based on six plausible diagnoses without changing
the reported symptom.

The next pass is DIAGNOSTIC ONLY.

Do not modify updateObj, syncXform, selection semantics, transform maths,
renderer lifecycle, canvas stacking, pointerEvents, or the toolbar implementation
until the runtime observation below identifies which layer is failing.

==================================================
GOAL
==================================================

Determine exactly where the Scene toolbar interaction stops:

    browser pointer event
        ↓
    React toolbar onClick
        ↓
    selection available
        ↓
    nudge handler
        ↓
    updateObj
        ↓
    scene state
        ↓
    syncXform
        ↓
    THREE mesh

We need the FIRST failed boundary.

==================================================
TEST 1 — DOES REACT RECEIVE THE TOOLBAR CLICK?
==================================================

Temporarily make the ROTATE mode button handler:

    onClick={(e) => {
        console.log('[SCENE TOOLBAR] ROTATE CLICK', {
            target: e.target,
            currentTarget: e.currentTarget,
            selCount: selIds.size
        });
        setSceneXform('rotate');
    }}

Do the equivalent minimal log for:

    SCALE
    X axis
    + button

Do not change behaviour otherwise.

Also add:

    onPointerDown={(e) => {
        console.log('[SCENE TOOLBAR] POINTER DOWN', e.target);
    }}

to the OUTER toolbar container.

Run the app and click ROTATE.

There are only two useful outcomes.

--------------------------------------------------
OUTCOME A
--------------------------------------------------

No:

    [SCENE TOOLBAR] POINTER DOWN

appears.

STOP.

The transform/state code is irrelevant because the browser event never reaches
the React toolbar.

Proceed to TEST 2.

--------------------------------------------------
OUTCOME B
--------------------------------------------------

POINTER DOWN and ROTATE CLICK appear.

STOP investigating canvas interception.

Proceed directly to TEST 3.

==================================================
TEST 2 — POINTER HIT TEST / CANVAS STACKING
==================================================

Only perform this if TEST 1 receives NO toolbar pointer event.

At runtime, inspect:

    document.elementFromPoint(x, y)

using coordinates in the middle of the visible ROTATE button.

Log the result and its ancestry.

Also inspect computed styles for:

    toolbar container
    rotate button
    WebGL canvas
    mountRef div

Specifically record:

    pointer-events
    position
    z-index

Do not infer from JSX.

Use the ACTUAL computed DOM state.

Expected healthy result:

    elementFromPoint(button centre)

must resolve to the button or one of its children.

If it resolves to:

    canvas

then we have finally demonstrated the bug.

ONLY THEN fix stacking/pointer ownership.

Preferred minimal fix:

    toolbar:
        position: absolute
        zIndex: sufficiently above canvas
        pointerEvents: 'auto'

and ensure the canvas does not establish a stacking arrangement that places it
above the controls.

Do NOT set:

    canvas.style.pointerEvents = 'none'

because the Scene viewport requires canvas pointer events for:

    picking
    dragging
    orbit
    pan
    texture/face interaction

The solution must make the toolbar win only over its own screen area.

After the fix rerun:

    elementFromPoint(button centre)

and require it to return the toolbar/button.

==================================================
TEST 3 — DOES SELECTION EXIST?
==================================================

Only perform this if toolbar pointer events ARE reaching React.

Click a model once.

Read the visible toolbar header.

Report EXACTLY:

    SELECT

or:

    NUDGE 1

Do not paraphrase this as "selected".

Also log:

    console.log('[SCENE SEL]', {
        selId,
        selIds: [...selIds],
        size: selIds.size
    });

Expected:

    selId = clicked object id
    selIds = [same id]
    size = 1

If header remains SELECT / size 0:

STOP.

The selection repair is still not functioning at runtime.

Investigate only that path.

If header becomes NUDGE 1:

selection is proven working.

Proceed to TEST 4.

==================================================
TEST 4 — DOES MODE STATE CHANGE?
==================================================

With:

    NUDGE 1

visible, click ROTATE.

The button must visibly enter its active style AND log:

    [SCENE TOOLBAR] ROTATE CLICK

Then log sceneXform from an effect:

    useEffect(() => {
        console.log('[SCENE XFORM STATE]', sceneXform);
    }, [sceneXform]);

Expected:

    rotate

If click log occurs but sceneXform remains unchanged:

investigate the state setter/binding.

If it becomes rotate:

proceed.

Do the same for X axis.

Expected:

    sceneAxis === 'x'

==================================================
TEST 5 — DOES + ENTER THE NUDGE HANDLER?
==================================================

Click + exactly once.

At the FIRST line of the + handler log:

    console.log('[SCENE NUDGE ENTER]', {
        mode: sceneXform,
        axis: sceneAxis,
        ids: [...selIds],
        count: selIds.size
    });

Expected:

    mode: rotate
    axis: x
    count: 1

If this log does not appear:

the + button/event wiring is the failure.

Do not inspect updateObj.

If it appears:

proceed.

==================================================
TEST 6 — DOES THE LOOP FIND THE OBJECT?
==================================================

Inside:

    selIds.forEach(id => ...)

log:

    console.log('[SCENE NUDGE OBJECT]', {
        id,
        found: !!o,
        rx: o?.rx,
        ry: o?.ry,
        scale: o?.scale,
        x: o?.x,
        y: o?.y,
        z: o?.z
    });

For ROTATE X + expect:

    found: true

and then log the exact proposed call:

    [SCENE NUDGE CALL] id rx OLD -> OLD+5

If found=false:

STOP.

Investigate object lookup only.

If found=true:

proceed.

==================================================
TEST 7 — DOES updateObj RUN?
==================================================

Temporarily add at the absolute beginning of updateObj:

    console.log('[UPDATEOBJ ENTER]', { id, key, val });

ROTATE X + must produce:

    key: 'rx'

SCALE + must produce:

    key: 'scale'

MOVE X + must produce:

    key: 'x'

If SCENE NUDGE CALL appears but UPDATEOBJ ENTER does not:

we have a call/reference problem.

If UPDATEOBJ ENTER appears:

proceed.

==================================================
TEST 8 — STATE + LIVE THREE VALUE
==================================================

For one object start with:

    rx = 0
    scale = 1

ROTATE / X / +

Immediately after the update has committed, report:

    scene object's rx
    objsRef object's rx
    threeRef.current.meshMap[id].rotation.x

Expected:

    scene rx = 5
    objsRef rx = 5
    THREE rotation.x ≈ 0.08726646

Then SCALE / +.

Expected:

    scene scale = 1.1
    objsRef scale = 1.1
    THREE scale.x = 1.1
    THREE scale.y = 1.1
    THREE scale.z = 1.1

Do not use visual appearance as the test.

Read the values.

==================================================
IMPORTANT: DO NOT FIX THE FIRST SUSPICIOUS THING YOU SEE
==================================================

Complete the diagnostic chain until you can state:

    LAST PASS:
        <specific boundary>

    FIRST FAIL:
        <specific boundary>

Example:

    LAST PASS:
        browser pointer reaches toolbar container

    FIRST FAIL:
        ROTATE React onClick never fires

or:

    LAST PASS:
        updateObj receives rx=5

    FIRST FAIL:
        scene object remains rx=0

That is the evidence required before changing code.

==================================================
ABOUT THE CANVAS THEORY
==================================================

The current strongest untested lead is:

    React renders toolbar inside mountRef
    Three effect later appendChild(canvas) to mountRef

so canvas is a later DOM sibling.

The toolbar visually appears above the canvas because it has zIndex:5.

That DOES NOT by itself prove which element wins hit-testing.

Do not "fix pointer events" based on that reasoning.

Prove it with:

    toolbar onPointerDown

and:

    document.elementFromPoint(...)

first.

==================================================
DO NOT REVERT YET
==================================================

Do not revert the Scene toolbar yet.

The toolbar implementation may be completely correct and merely unreachable
from pointer input.

A diagnostic this small is cheaper and more informative than deleting the work.

If the diagnostic shows the architecture is fundamentally fighting the canvas,
THEN we can decide whether to remove it and rebuild from the working RIG
interaction pattern.

==================================================
REPORT BACK ONLY THE OBSERVATIONS
==================================================

Return this table:

TEST 1 toolbar pointerdown:
TEST 1 rotate onclick:

TEST 2 elementFromPoint:
TEST 2 toolbar pointer-events:
TEST 2 canvas pointer-events:
TEST 2 toolbar computed z-index:
TEST 2 canvas computed z-index:

TEST 3 header:
TEST 3 selId:
TEST 3 selIds:

TEST 4 sceneXform after ROTATE:
TEST 4 sceneAxis after X:

TEST 5 nudge handler entered:
TEST 5 mode/axis/count:

TEST 6 object found:
TEST 6 proposed key/value:

TEST 7 updateObj entered:
TEST 7 received key/value:

TEST 8 scene stored value:
TEST 8 objsRef value:
TEST 8 THREE value:

LAST PASS:
FIRST FAIL:

If a test fails, later tests that depend on it can be marked NOT REACHED.

Do not make a seventh functional change until FIRST FAIL is established.
