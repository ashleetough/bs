ROOT CAUSE FOUND. Do not redesign the migration helpers.

The recursion is REAL and directly visible in the current source.

Inside:

    function assertRigMigrationLossless() {

the function contains:

    assertRigMigrationLossless();

inside its own try block before the BAS comparison.

That is unconditional self-recursion.

It repeatedly calls itself until JavaScript throws:

    Maximum call stack size exceeded

The suspected cycle:

    normalizeRigInput
      -> migrateLegacyRig
      -> normalizeRigInput

does NOT exist.

==================================================
1. REMOVE THE ACCIDENTAL SELF-CALL
==================================================

Delete ONLY this line from inside assertRigMigrationLossless():

    assertRigMigrationLossless();

Do not change migrateLegacyRig, normalizeRigInput, or rigToLegacyParts as part
of this fix.

After removal, the function should flow directly:

    fixture
      ->
    generate legacy BAS
      ->
    migrate
      ->
    flatten migrated rig
      ->
    generate migrated BAS
      ->
    byte comparison

==================================================
2. MAKE THE GATE FAIL CLOSED
==================================================

Yes: this gate should THROW on failure, not quietly return false.

It is being treated as a safety invariant for the rig migration.

A log-only false result is too easy for callers to ignore.

Preferred structure:

    function assertRigMigrationLossless() {
      const fixture = [...];

      const legacy = generateRigBAS(...);
      const migrated = generateRigBAS(...);

      if (legacy !== migrated) {
        find first differing line;
        throw new Error(
          `[RIG] migration is not lossless at line ${n}: ...`
        );
      }

      return true;
    }

Do NOT swallow its own exception inside the assertion.

An assertion that catches itself and converts failure to false is not really an
assertion.

If UI code wants graceful handling, catch it at the boundary that invokes the
gate:

    try {
      assertRigMigrationLossless();
    } catch (err) {
      console.error(err);
      disable RIG export / surface a visible error;
    }

The checker itself should remain strict.

==================================================
3. DO NOT RUN IT RECURSIVELY OR FROM THE GENERATOR
==================================================

Audit call sites after removing the self-call.

There should be no invocation of assertRigMigrationLossless() from:

    generateRigBAS
    migrateLegacyRig
    normalizeRigInput
    rigToLegacyParts

Those functions must stay pure with respect to the safety check.

The gate should call THEM.

They should never call the gate back.

That gives a one-way dependency:

    assertion
       -> generator
       -> migration/helpers

never:

    generator/helper
       -> assertion

==================================================
4. FIXTURE GATE IS STILL THE RIGHT PERMANENT REGRESSION TEST
==================================================

Keep the fixture-based exact BAS comparison.

It tests important structural cases:

    amplitude 0 continuous spin
    normal swing
    multi-axis X+Z animation
    static part
    parent hierarchy
    offsets
    scale

That makes it a good deterministic regression test.

Do not replace it with user-rig testing.

A permanent assertion should be:

    deterministic
    reproducible
    independent of whatever project happens to be open

==================================================
5. ADD ACTUAL-RIG VERIFICATION ONLY DURING LEGACY LOAD
==================================================

The user's actual rig IS useful as a SECOND safety check, but only when loading
a legacy flat rig.

That is the ideal point because both representations are available:

    original legacy rigParts
    migrated rig

Before discarding the legacy form:

    legacyBAS = generateRigBAS(originalLegacyParts, ...)
    migratedBAS = generateRigBAS(
        rigToLegacyParts(migratedRig),
        ...
    )

Compare them.

If they differ:

    DO NOT silently continue migration
    preserve the original legacy data
    surface an error
    refuse to overwrite/save as v2 until resolved

This catches cases outside the fixed fixture.

But do NOT run this comparison on every normal load of an already-v2 rig.

For:

    rigVersion >= 2

there is no legacy source to compare against and no migration is occurring.

==================================================
6. SEPARATE TWO DIFFERENT GATES
==================================================

Use two explicit concepts:

A. DEVELOPER REGRESSION GATE

    assertRigMigrationFixture()

Runs against a known fixture.

Purpose:
    prove code changes did not alter migration/export compatibility.

B. LEGACY PROJECT MIGRATION CHECK

    verifyLegacyRigMigration(legacyParts, migratedRig, exportSettings)

Runs only when actually upgrading old project data.

Purpose:
    prove THIS USER'S rig survived migration.

Do not conflate them.

==================================================
7. IMPORTANT CAVEAT ABOUT "BYTE IDENTICAL"
==================================================

The current migration creates IDs with:

    Math.random()

when a legacy part has no id.

That does not currently affect generateRigBAS because the BAS generator does
not emit those IDs.

So BAS equality can still be deterministic.

But the migration OBJECT itself is not deterministic.

For a permanent regression fixture, prefer deterministic generated part IDs.

Example:

    id: p.id || `part_${i}`

or another stable migration-specific ID.

Random IDs make:

    save diffs
    migration snapshots
    reproducibility
    test debugging

needlessly noisy.

Do NOT change this if existing saved v2 rigs already depend on those IDs in a
way that would break references.

But for newly migrated legacy rigs, stable IDs are preferable.

Inspect track partId/reference semantics before changing it.

==================================================
8. ALSO HARDEN normalizeRigInput
==================================================

Current behavior:

    if Array        -> migrate
    if rigVersion>=2 -> accept
    otherwise       -> migrateLegacyRig([])

That last branch silently converts a malformed/unknown object into an EMPTY rig.

That is dangerous.

For example:

    { skeleton: ..., clips: ... }

with accidentally missing rigVersion becomes:

    empty rig

instead of producing an error.

Do NOT necessarily change it in the same commit as the recursion fix unless
safe, but flag it as the next correctness cleanup.

Preferred eventually:

    if Array.isArray(input):
        legacy migrate

    if input && input.rigVersion >= 2:
        validate and return

    if input == null:
        return new empty default rig

    otherwise:
        throw new Error("Unknown rig data shape")

Do not silently destroy unknown rig-shaped data.

==================================================
9. TEST THE FIX DIRECTLY IN THE APP
==================================================

Do not rely on the eval harness for the decisive test.

After removing the self-call, run in the actual application environment:

    const result = assertRigMigrationLossless()

Expected:

    true

and NO console:

    Maximum call stack size exceeded

Then deliberately break one migration field temporarily in a local test, for
example alter migrated speed.

Expected:

    assertion THROWS
    first differing BAS line is reported

Then restore it.

This proves both pass and fail behavior.

==================================================
10. HASH THE OUTPUTS FOR DIAGNOSTICS
==================================================

Optionally log:

    legacy BAS length
    migrated BAS length
    hash legacy
    hash migrated

plus:

    exact equality PASS

Do not use hash equality instead of string equality.

The string comparison remains authoritative.

Hashes are just a compact diagnostic.

==================================================
11. EXPORT POLICY
==================================================

Yes: if the permanent fixture gate fails in the running build, disable RIG
export.

Reason:

The application no longer knows that its migration/export semantics are safe.

Do not block unrelated Scene/DOOM exports unless they use this rig migration.

Scope the failure to RIG.

Recommended UI:

    RIG MIGRATION INTEGRITY FAILED
    Export disabled.
    <first difference/error>

This is better than producing a possibly-corrupt RIG.ZIP after merely logging
false.

==================================================
12. DO NOT PROCEED TO KEYFRAMES OR CLIP SWITCHING
==================================================

Keep those blocked until all of these pass:

    fixture gate PASS
    no recursion
    deliberate-failure test THROWS
    real legacy rig migration comparison PASS
    already-v2 rig loads without migration
    RIG export enabled only when integrity gate passes

No keyframes.
No generated clip switching.
No new rig schema work until this is trustworthy.

==================================================
REPORT BACK
==================================================

Return:

1. confirmation accidental self-call removed
2. fixture gate result in real app
3. whether any stack overflow remains
4. deliberate migration-break test result
5. first-difference reporting result
6. actual legacy user-rig comparison result
7. whether already-v2 loads skip migration comparison
8. export blocking behavior on gate failure
9. normalizeRigInput malformed-object behavior
10. whether random part IDs were retained or made deterministic

Do not change the rig animation model in this pass.
