RIG mounts with no parts
↓
usePreviewViewport builds THREE scene
↓
sceneOverride at build time = null/empty
↓
grid renders, zero meshes
↓
preset creates six parts + six cached geometries
↓
RigWorld computes six new objs
↓
sceneOverrideRef updates
↓
BUT mesh-building loop NEVER RUNS AGAIN
↓
grid forever
