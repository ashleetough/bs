We are now firmly in CATEGORY E: generated BASIC is clean.

Do NOT change facing thresholds, statement ordering, animation architecture,
or add another speculative FRONT fallback yet.

The next test is the existing runtime probe:

    EVA <value>  EAF <value>  EHM <value>

Run the game with enemy 1 visibly showing the incorrect SIDE sprite and report
those THREE VALUES while it is happening.

We need the measurement, not another code change.

==================================================
INTERPRETATION
==================================================

CASE A
    EVA is between -0.5 and +0.5

Then SIDE is being selected intentionally by the generated BASIC.

The sprite/atlas/animation system is NOT the immediate problem.

Trace the facing maths.

In particular, EVA currently measures:

    enemy MOVEMENT VECTOR
            dot
    enemy -> player vector

using:

    EHX = EX1-ELX1
    EHY = EY1-ELY1

This is not necessarily the enemy's actual facing direction.

If movement/collision resolution makes the enemy move laterally, axis-by-axis,
or slide along geometry, EVA can legitimately approach zero even though the
enemy is conceptually chasing/facing the player.

If CASE A occurs, also print:

    EHX
    EHY
    SPX
    SPY

for enemy 1.

Do not fix anything until those values are known.

==================================================

CASE B
    EVA > 0.5
    but SIDE is visibly displayed

Then the SIDE condition should be false.

This becomes a runtime control-flow / SPRTEX-state problem.

Extract the EXACT final generated enemy-1 texture-selection block, in execution
order, including every:

    SPRTEX 1
    EVA condition
    EAF1 condition
    EHT1 condition
    attack condition

Then identify the LAST SPRTEX 1 that executes during that frame.

Do not reorder anything yet.

==================================================

CASE C
    EAF1 remains 0

Then separately inspect animation timing.

Print consecutive values for:

    EAT1
    EAF1

over at least 30 game iterations.

Expected:

    EAT1  0..6    -> EAF1 0
    EAT1  7..13   -> EAF1 1
    EAT1 14..20   -> EAF1 2
    EAT1 21..27   -> EAF1 3
    then reset

If EAT increments but EAF remains zero, investigate expression/runtime
evaluation.

If BOTH remain effectively static, investigate whether those animation
statements execute each game iteration.

==================================================

CASE D
    EVA > 0.5
    and EAF1 cycles 0,1,2,3
    but displayed texture remains SIDE

This is extremely useful.

At that point facing maths and animation counter are both innocent.

Instrument the actual texture selection.

Add a temporary variable:

    EDF=...

representing which texture branch most recently executed:

    FRONT frame 1 -> distinct diagnostic value
    FRONT frame 2 -> distinct diagnostic value
    FRONT frame 3 -> distinct diagnostic value
    FRONT frame 4 mapping -> distinct diagnostic value
    SIDE -> distinct diagnostic value
    HURT -> distinct diagnostic value
    ATTACK -> distinct diagnostic value

Set EDF on the SAME condition as each corresponding SPRTEX.

Display EDF.

This tells us which BASIC branch actually wins without inferring it from the
sprite artwork.

==================================================
IMPORTANT: DO NOT "FIX" FRONT YET
==================================================

Do not add an unconditional FRONT SPRTEX merely because animated FRONT may have
replaced the previous bare default.

EAF1 is mathematically constrained to integer states 0..3 if EAT1 behaves as
intended, so four correctly emitted EAF branches already provide complete
coverage.

Adding another default could conceal the actual failure.

Likewise, do NOT move SIDE before FRONT.

The current intended precedence is reasonable:

    FRONT selection
        ->
    SIDE override when EVA is in side band
        ->
    hurt/attack/state overrides

If SIDE currently wins incorrectly, measure WHY.

==================================================
ONE EXTRA STATIC CHECK
==================================================

Before changing code, show the exact FINAL generated enemy-1 block.

Not the generator template.

I specifically want to see whether the old unconditional:

    SPRTEX 1,<front1>

still exists in addition to the EAF-gated FRONT lines, or whether animation
replaced it.

Report that as:

    unconditional FRONT present: YES/NO

and list the four EAF1 -> texture mappings.

==================================================
DO NOT TOUCH
==================================================

- BSIM/BSPL writers
- DDA
- WXF/TXC
- wall projection
- atlas allocation
- z ordering
- facing thresholds
- SPRTEX ordering
- animation architecture

First return:

1. EVA while SIDE is visibly showing
2. EAF1 at that moment
3. EHM at that moment
4. unconditional FRONT present YES/NO
5. EAF1 0/1/2/3 texture mappings

If EVA is in the SIDE band, additionally return EHX/EHY/SPX/SPY.

Follow that measured branch only.
