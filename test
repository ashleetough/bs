Recover stability first. Do not continue from the currently broken renderer.

==================================================
1. REVERT TO THE LAST KNOWN-GOOD BUILD
==================================================

Return to:

    68b77521f5e8f266

That is the current trusted baseline.

Known behaviour there:

- all 128 wall strips render
- half-wall flag works
- half walls render at half height
- half-wall base stays on the floor
- no transparent overlay exists
- half walls still occlude geometry behind them

That occlusion is acceptable for now.

Do NOT try to salvage the current partial-overlay build.

The current build has 28 wall strips missing SPRPOS2 / SPRDISP, so it is not a
valid debugging baseline.

==================================================
2. ADD HARD STRIP-COUNT ASSERTIONS IMMEDIATELY
==================================================

Before any further feature work, add structural assertions to the FINAL
generated BASIC.

For main wall-strip sprite IDs:

    200..327

there must be exactly:

    128 SPRPOS2 commands
    128 SPRDISP commands

and ideally one expected texture-selection block per ID.

Do not use a loose regex such as:

    SPRDISP 2\d\d

because that matches only 200..299 and misses 300..327.

Parse the numeric sprite ID and check the range semantically:

    id >= 200 && id <= 327

For every ID 200..327 assert individually:

    SPRPOS2 <id> exists exactly once in the wall-render block
    SPRDISP <id> exists exactly once

Then assert totals:

    main wall strips = 128

Fail the build loudly if any ID is missing.

Report the exact missing IDs.

==================================================
3. ADD A "DO NOT DAMAGE MAIN STRIPS" INVARIANT
==================================================

No later generator transform should be allowed to delete or rewrite arbitrary
main wall lines based on substring matching.

Especially forbid cleanup patterns shaped like:

    remove every line containing QH...
    replace every SPRDISP 2...

unless the transform first proves the exact intended match set.

For any transformation intended to touch 128 rays:

    expected matches = 128

If actual != expected:

    abort transformation
    console.error
    keep original BAS unchanged

Never proceed with a partial 100/128 transformation.

==================================================
4. KEEP HALF WALLS SOLID FOR NOW
==================================================

Do NOT implement transparent half-wall continuation in this recovery build.

Current design constraint:

    HALF WALL = shorter opaque wall

Therefore:

- its visible height is half
- it blocks ray continuation
- things behind it are occluded

Treat that as a documented engine limitation for now, not a bug.

This gives us:

    correctness
    lower interpreted cost
    lower BASIC line count
    fewer sprite primitives
    simpler Z behaviour

Do not spend the remaining line budget solving it yet.

==================================================
5. WHY THE OVERLAY IS DEFERRED
==================================================

The transparent-half-wall architecture itself is valid:

    DDA encounters half wall
    remember hit
    continue ray
    find far wall
    render far wall
    render half wall over it at nearer depth

But it has meaningful cost:

- extra DDA work
- extra per-ray state
- extra wall-strip sprites
- additional generated BASIC
- more render work in the current bottleneck

The failed implementation reached approximately:

    19,024 / 20,000 lines

That is too close to the source limit for a feature that can wait.

Do not optimise around that limit yet.

==================================================
6. IF TRANSPARENT HALF WALLS RETURN LATER
==================================================

Build them INSIDE the ray/strip generator.

Do not add them by searching generated BASIC afterward.

The generator already knows:

    ray index
    main strip sprite ID
    screen X
    projection variables

For each of the 128 rays, generate together:

    MAIN STRIP
    optional HALF OVERLAY STRIP

By construction this guarantees:

    128 main strips
    128 overlay opportunities

No regex discovery pass is required.

Use a deterministic overlay sprite range, for example:

    main:    200..327
    overlay: 328..455

after verifying those IDs remain unused.

==================================================
7. ASSERT OVERLAY COUNTS TOO
==================================================

When that future feature is implemented:

For IDs 328..455 assert:

    128 overlay SPRPOS2 forms
    128 overlay SPRDISP forms

even if runtime collapses/hides many of them.

Generation structure should still be complete for all rays.

Do not rely on SPROFF in the wall-strip path if trailing SPRDISP revives the
sprite.

Use the already established collapse mechanism where appropriate:

    SY2 = SY1

==================================================
8. NO REGEX CLEANUP BY VARIABLE PREFIX
==================================================

The QH cleanup regression proves this is unsafe.

Do not delete generated lines just because they contain a temporary variable
name or prefix.

If temporary overlay code must be removed, remove it structurally from the
generator that emitted it.

Do not sanitize the final BAS with broad textual deletion.

==================================================
9. KEEP THE HALF-WALL WORK THAT IS NOW PROVEN
==================================================

Preserve the known-good half-height behaviour:

    HC array
    per-cell flag
    WH halving
    floor-pinned base

Do not return to:

    HC$
    centered half walls
    paint-time-only shape capture
    stale half metadata

Those were real bugs already fixed.

==================================================
10. BUILD REPORT
==================================================

Extend ? CHECK / build diagnostics to include:

    MAIN WALL STRIPS
      expected 128
      SPRPOS2 128
      SPRDISP 128
      missing IDs none

    HALF WALL MODE
      solid/opaque

    BASIC LINES
      N / 20000

This should be visible before RUN.

If a strip count fails:

    RUN should not silently continue.

Surface the error clearly.

==================================================
11. ACCEPTANCE TEST AFTER REVERT
==================================================

Before any new feature work verify:

A.
    normal full wall
    -> renders correctly

B.
    half wall
    -> renders at half height
    -> bottom sits on floor
    -> all screen columns remain intact

C.
    rotate through full 360-degree view
    -> no 28-column missing band
    -> no translucent vertical streaks

D.
    final BAS:
        128 SPRPOS2 for ids 200..327
        128 SPRDISP for ids 200..327

E.
    line count comfortably below 20,000 again

Only after all pass mark the renderer stable.

==================================================
12. ARCHITECTURE RULE GOING FORWARD
==================================================

Anything that exists once per ray should be generated once per ray.

Do not reconstruct ray structure afterward by pattern-matching final BASIC.

Examples:

    wall strips
    half-wall overlays
    future door overlays
    per-ray diagnostics

belong in the same generator loop / ray emitter.

Generated BASIC should be the OUTPUT of the structure, not the database from
which we rediscover that structure later.

==================================================
DO NOT TOUCH
==================================================

Do not change:

- BSIM/BSPL
- DDA mathematics
- WXF/TXC
- atlas allocation
- z ordering
- half-wall projection that is already working

Inline IF only.
132 chars maximum per line including line number.
No residual labels.
Do not rely on SPROFF in the wall-strip block.

==================================================
REPORT BACK
==================================================

Return:

1. confirmation reverted to 68b77521f5e8f266
2. final line count
3. SPRPOS2 count for ids 200..327
4. SPRDISP count for ids 200..327
5. missing main-strip IDs, if any
6. half-wall visual test
7. 360-degree render test
8. assertion implementation
9. confirmation transparent overlay is deferred
