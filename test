The screenshots materially change the diagnosis.

DO NOT describe this as "zero textures" or a total texture-pipeline failure.

The RUN screenshot visibly contains red/grey patterned wall textures.

Those are the generated wall PLACEHOLDERS.

Therefore these stages are demonstrably alive:

    buildDoomBAS completes
    runRaycaster completes
    buildTexturePages completes
    page -> PNG/data URL encoding completes
    _PS_TEXTURES reaches the emulator
    SPRTEX/drawImage is rendering texture data

So a diagnostic throwing inside buildDoomBAS is NOT the leading explanation.

Do not revert the last two diagnostic commits merely because they are new.

==================================================
IMPORTANT CODE FACT: SINGLE-PAGE SHEET IMPORT
==================================================

Inspect the current importSheet().

For a 512x256 upload:

    nPages = 1

It creates:

    out.page1

then does:

    setSpritePages(p => ({...p, ...out}))

Therefore a page-1 upload:

    REPLACES page1
    PRESERVES existing page2/page3/page4

It does NOT clear the whole atlas.

So "uploading page 1 deleted all texture pages" is disproved by the current
implementation.

Do not change this until runtime state says otherwise.

==================================================
WHAT THE SCREENSHOT ACTUALLY SUGGESTS
==================================================

The painter shows the user's Baron art.

The RUN shows generated placeholder textures.

That means the useful question is now:

    Why does buildTexturePages consider the CURRENT game cells unpainted
    even though the painter shows user art?

This is a provenance/layout problem until proven otherwise.

Remember buildTexturePages does:

    start with raw spritePages
    for each CURRENT atlasLayout cell:
        resolve cellToRect
        inspect alpha in that exact rect
        if painted:
            leave user pixels
        else:
            insert placeholder

So if a semantic frame moved but the old pixels remain elsewhere on the page:

    painter sheet may visibly contain artwork
    BUT current semantic cell may be empty
    -> placeholder gets composed
    -> RUN shows placeholder

That is exactly the class of problem already discovered after the animation
layout changed.

==================================================
DO NOT TRUST "I CAN SEE IT ON THE SHEET"
==================================================

A pixel being visible somewhere in the navigator is NOT proof that:

    enemy1:front1

currently owns those pixels.

The current allocator is semantic:

    resource id + frame
        ->
    atlasLayout cell
        ->
    cellToRect
        ->
    page/x/y

Measure that exact rectangle.

==================================================
ADD ONE "TEXTURE PROVENANCE" REPORT
==================================================

Do not add more BASIC instrumentation.

Add a browser-side diagnostic:

    ? TEX

It should inspect the exact state that RUN is about to use.

For EVERY live/wired resource cell, print:

    semantic key
    current page
    current rect
    raw spritePages alpha count
    composed-page alpha count
    source = PAINTED or PLACEHOLDER

Example:

    enemy1:front1
      p1 x0 y0
      raw alpha 1842
      composed alpha 1842
      PAINTED

    enemy1:front2
      p1 x64 y0
      raw alpha 1760
      composed alpha 1760
      PAINTED

    wall1:idle
      p4 x...
      raw alpha 0
      composed alpha 4096
      PLACEHOLDER

This report must use:

    CURRENT atlasLayout
    cellToRect()
    CURRENT spritePages
    buildTexturePages()

Do NOT recompute positions with row*size or another independent formula.

==================================================
REPORT THESE SPECIFIC CELLS FIRST
==================================================

For the resource currently used by enemy slot 1, report:

    enemy1:front1
    enemy1:front2
    enemy1:front3
    enemy1:side1
    enemy1:hurt1

Then:

    wall1:idle
    wall2:idle
    wall3:idle
    wall4:idle
    door:idle
    gun:idle

For each, give:

    page
    x/y
    nontransparent raw pixels
    placeholder yes/no

This should explain the screenshot immediately.

==================================================
COMPARE PAINTER SELECTION AGAINST CURRENT LAYOUT
==================================================

While FRONT1 is selected in the painter, display:

    selected semantic key
    selected atlas page
    selected x/y

Example:

    enemy1:front1
    PAGE 1
    X 0 Y 0

Then confirm the Baron pixels actually occupy THAT rectangle.

Do the same for FRONT2 and FRONT3.

Do not merely look at thumbnails.

If the thumbnails are reading an old coordinate or a different resource,
we need to know.

==================================================
THERE IS STILL MULTI-PAGE POSITIONAL CODE TO AUDIT
==================================================

I inspected the current JSX and some slot operations still calculate destination
coordinates directly from:

    sl.row * SPRITE_SIZE
    sl.col * SPRITE_SIZE

rather than using cellToRect()'s LOCAL page row/y.

Examples include crop/import/clear-style code paths.

That is dangerous for any slot whose global row is >= rowsPerPage.

The painter's main origin was fixed to:

    cellToRect(slotDef)

but not every write path has necessarily been converted.

Audit EVERY spritePages write operation and classify it:

    SAFE:
        uses pageKey from cellToRect
        AND x/y from the SAME cellToRect

    BROKEN FOR MULTIPAGE:
        pageKey is page N
        but pixel offset uses global sl.row*SPRITE_SIZE

For a page-2 cell, this can become:

    page2 buffer
    y = 256+

which writes outside the logical 256px page.

Specifically audit:

    draw brush
    erase
    clearSlot
    commitCropWhole
    commitCrop
    pasteSlot
    duplicateToNext
    shift/nudge
    centre
    fill selection
    sheet import
    undo/redo

Do NOT blindly patch all of them first.

Report which ones mix:

    page-aware buffer
with
    global row coordinates.

This is highly relevant because the page-key fix changed one half of this
relationship.

==================================================
VERY IMPORTANT DISTINCTION
==================================================

The page-key fix may itself have EXPOSED previously hidden bad coordinate math.

Before:

    wrong page + global coordinates

could accidentally look internally consistent in the painter.

After:

    correct page + stale global row coordinates

can write off-page.

That would explain why the behavioural change appeared around the page-key fix
without meaning the page-key idea itself was wrong.

The invariant must be:

    page, x, y

ALL come from the SAME cellToRect(cell).

Never:

    page from cellToRect
    y from cell.row * size

==================================================
CHECK importSheet SEMANTICS
==================================================

The current one-page upload intentionally preserves later pages:

    setSpritePages(p => ({...p, ...out}))

Keep that behaviour for now.

But add a toast/report saying explicitly:

    Imported page1 only; pages 2-4 preserved

when nPages=1 and later pages already exist.

The existing wording "Sheet loaded — 1 page" can easily make the user think
the whole atlas now contains one page.

Also report:

    raw pages after import:
    page1 yes
    page2 yes
    page3 yes
    page4 yes

No guessing.

==================================================
CHECK THE RUN COMPOSITION DIRECTLY
==================================================

Immediately before assigning _PS_TEXTURES, report:

    buildTexturePages pages present
    placeholders inserted

Example:

    RUN TEX:
      page1 yes
      page2 yes
      page3 yes
      page4 yes
      placeholders 17

This is nearly free because runRaycaster already calls buildTexturePages.

If all four pages exist, the page-1 upload did not destroy anything globally.

==================================================
THE FALSE WALL WARNINGS SHOULD BE FIXED
==================================================

The existing build-time wall "EMPTY" assertion reads raw spritePages.

But placeholders are applied later by buildTexturePages.

So:

    raw wall empty

does NOT mean:

    exported/run wall empty

That warning is now known to lie.

Either:

A. rename it explicitly:

    wall has NO USER ART — placeholder will be used

or preferably:

B. run the assertion against the composed buildTexturePages result and report:

    USER ART
    PLACEHOLDER
    ACTUALLY EMPTY

Do not leave "EMPTY" as an error/warning when the intended placeholder path
will populate it.

==================================================
DO NOT REVERT YET
==================================================

Do NOT jump back to b7ccdf6da47f9076 yet.

A revert bundles multiple state/layout differences and would tell us little.

First obtain:

1. current raw page keys
2. current semantic-cell raw alpha counts
3. composed source PAINTED/PLACEHOLDER
4. audit of mixed page/global-row writes

Those measurements are more informative than another build bisect.

Only bisect if those all look correct.

==================================================
LIKELY OUTCOMES
==================================================

CASE A

    enemy1 front raw alpha = 0
    composed = placeholder

Then artwork is simply not at the CURRENT semantic coordinates.

This is the layout-orphan problem.

Recover/migrate the art; do not debug renderer.

CASE B

    raw alpha > 0
    composed = painted
    but RUN still shows placeholder

Then buildTexturePages isn't the object actually reaching RUN, despite the
current code suggesting it is.

Trace _runPages -> texMap.

CASE C

    page1 correct
    page2/3/4 missing after import

Then actual runtime state contradicts importSheet source.

Trace setSpritePages lifecycle/effects for a later replacement/reset.

CASE D

    page2+ cells show zero raw pixels after crop/import actions
    and those writers use sl.row*SIZE

Then the page-key fix exposed stale global-coordinate writes.

Fix every page-aware writer to use cellToRect consistently.

==================================================
DO NOT TOUCH
==================================================

Do not touch:

- BSIM/BSPL
- DDA
- WXF/TXC
- wall projection
- z ordering
- BASIC animation maths
- facing thresholds
- texture renderer

The screenshots already demonstrate that the renderer can receive and display
textures.

This round is about:

    semantic cell
        ->
    raw page coordinates
        ->
    composed page provenance

==================================================
REPORT BACK
==================================================

Return:

1. raw spritePages keys immediately after the page-1 upload
2. page count entering RUN
3. placeholder count entering RUN
4. provenance for enemy1 front1/front2/front3/side1
5. provenance for wall1..4/door
6. selected FRONT1 semantic key + current page/x/y
7. whether its visible Baron pixels occupy that exact rect
8. complete list of sprite write functions still using global row*SIZE
9. whether any of those target page >1
10. only then recommend the smallest fix

Do not ship another speculative build before these measurements.
